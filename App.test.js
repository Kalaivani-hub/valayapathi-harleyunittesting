import React from "react";
import { render, screen } from '@testing-library/react';
import App from './App';

//import { shallow } from "enzyme";
//import Enzyme from 'enzyme';
//import Adapter from "@cfaester/enzyme-adapter-react-18";

//configure({ adapter: new Adapter() });



//     describe("Counter Testng", () =>{

  //test("render the  title of counter", () => {

    // const wrapper = shallow(<stories />);
    //expect(wrapper.find("h1").text()).toContain("Stories of Freedom");
  //});
  
  //test("render a button with text of 'increment'",()=>{

    //const wrapper = shallow(<stories />);
   //expect(wrapper.find('#click-btn').text()).toContain("Click here");
//});
//});

    //const { getByText  }=render(<App />);

    //const linkElement = screen.getByText('Stories of Freedom');

    //expect(linkElement).toBeInTheDocument();

   




   
